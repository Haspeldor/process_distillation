anpassungen:
daten neu labeln -> fine tuning
neu labeln vs regularisierung
eigene loss funktion
regularisierer aus white box

todo:
    - decision tree effizienter generieren
    
    - hospital datensatz
        - in df einlesen
        - attribute auswählen
        - laufen lassen

    - numerische attribute
        - beim auslesen dict verwenden, für korrekte codierung

    - transformer model verwenden anstatt MLP

    - state of the art per ssh

    - better shell for interactive mode